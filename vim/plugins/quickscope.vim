" Trigger a highlight in the appropriate direction when pressing these keys:
let g:qs_highlight_on_keys = ['f', 'F', 't', 'T']

" Deactivate on line longer than
let g:qs_max_chars=150

"let g:qs_lazy_highlight = 1
